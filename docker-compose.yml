services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: velozity
    ports: ["5432:5432"]
    volumes: ["velozity_pg:/var/lib/postgresql/data"]
volumes:
  velozity_pg:
